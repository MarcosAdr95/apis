{
  "name": "apis",
  "version": "1.0.0",
  "description": "Projeto para estudos que armazena requisições a diferentes apis",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "server": "node dist/index.js",
		"dev": "concurrently \"npx tsc --watch\" \"nodemon dist/index.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.0",
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1",
    "typescript": "^5.1.6"
  }
}
